# 魔窟 / Roku — Agent 约定

## 技能文案分层（已踩过坑，勿再混）

| 位置 | 显示 |
|------|------|
| 技能**列表** | **不写**说明/公式；只显示名称、类型、等级、升级 |
| 自动战斗选技能 | 可用短 `nums` |
| 技能**详情**弹窗 | 用完整 `desc`（目标/触发 + 伤害公式） |

完整介绍只出现在详情页（点进技能看）。列表不要出现描述句或公式行。

## 单位头像尺寸（只用这 4 档）

源：`js/ui/unitIcon.js`（`unitIconHtml` / `unitShapeHtml` / `unitDiamondScale`）

| 档位 | 盒宽 | 用途 |
|------|------|------|
| `xs` | 28px | 战斗信息列表 |
| `sm` | 40px | 探索左侧条、本层怪物列表、阵容格/池 |
| `md` | 56px | 战斗场地站位 |
| `lg` | 120px | 英雄详情立绘（`diamondScale`） |

不要再散落 `0.72` / `0.95` / `1.15` / `2.2` 之类随意缩放；新 UI 一律选上表档位。
