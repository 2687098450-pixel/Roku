# 魔窟 / Roku — Agent 约定

## 技能文案分层（已踩过坑，勿再混）

| 位置 | 显示 |
|------|------|
| 技能**列表**（`.skill-nums`） | 只用 `nums`（短公式/数值，如 `攻击力×1.65+6`、`敌防御力×60%+4 · 友×70%`） |
| 自动战斗选技能 | 只用 `nums` |
| 技能**详情**弹窗 | 用完整 `desc`（目标/触发 + **伤害怎么算**的公式） |

完整介绍只出现在详情页，不要为了「说清楚」把整句塞进列表。

伤害写法用计算公式（如 `防御力×60%+4`、`攻击力×1.15`），不要只写算完的整数。
